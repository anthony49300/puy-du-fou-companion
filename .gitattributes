# Normalise les fins de ligne en LF dans le dépôt, quel que soit l'OS local
# (évite les diffs bruyants entre Windows et Linux/macOS et les
# avertissements Git "LF will be replaced by CRLF").
* text=auto eol=lf

*.db binary
*.pdf binary
*.png binary
*.jpg binary
